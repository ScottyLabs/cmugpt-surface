[project]
name = "cmugpt-surface"
revision = "1.0"

[profiles.default]
OIDC_CLIENT_ID = { description = "Keycloak OIDC client ID", required = true }
OIDC_CLIENT_SECRET = { description = "Keycloak OIDC client secret", required = true }
[profiles.dev]

[profiles.prod]
OIDC_CLIENT_ID = { description = "Keycloak OIDC client ID (written by kennel on deploy)", required = true }
OIDC_CLIENT_SECRET = { description = "Keycloak OIDC client secret (written by kennel on deploy)", required = true }

[profiles.staging]
OIDC_CLIENT_ID = { description = "Keycloak OIDC client ID (written by kennel on deploy)", required = true }
OIDC_CLIENT_SECRET = { description = "Keycloak OIDC client secret (written by kennel on deploy)", required = true }

[profiles.preview]
OIDC_CLIENT_ID = { description = "Keycloak OIDC client ID (written by kennel on deploy)", required = true }
OIDC_CLIENT_SECRET = { description = "Keycloak OIDC client secret (written by kennel on deploy)", required = true }
