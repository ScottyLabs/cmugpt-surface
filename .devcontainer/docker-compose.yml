name: "cmugpt-frontend"

services:
  dev:
    container_name: cmugpt-frontend-dev
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    depends_on:
      - postgres

  postgres:
    image: postgres:17
    container_name: cmugpt-frontend-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: donotuseinprod
      POSTGRES_DB: cmugpt-frontend
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
